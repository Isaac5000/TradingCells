# Guia breve para agentes

Empieza por `PROJECT_INDEX.md` y abre solo las rutas de la feature implicada. No indexar `.gradle/`, `build/`, `logs/`, `run/` ni `run-server/`. Conserva siempre los cambios existentes del usuario y valida con las tareas indicadas en el indice.
