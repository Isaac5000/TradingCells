# Validacion de la candidata 1.0.0

## Bases opacas y encuadre de granjas configurables del 2026-08-31

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `26/26` GameTests y produjeron un JAR identico de
  `2.245.484` bytes con SHA-256
  `14752C37FE2D4C59B9062EAB838CBB0340C12BD9F1AE3414A7B065AFF8B3A103`.
- `check` valido 384 JSON, 64 PNG, 13 payloads, 104 claves NBT, 91 recetas,
  arquitectura, dominio, contenido del JAR, portabilidad y neutralidad
  OpenGL/Vulkan.
- Artropodos y Fantasmas usan musgo palido de forma coherente en bloque, objeto
  y receta. La granja de Slimes conserva `minecraft:slime_block` como ingrediente
  y usa una copia visual local con los mismos colores y alfa totalmente opaco.
- El Ghast y el Happy Ghast comparten un desplazamiento vertical adicional de
  `0,16` bloques respecto al resto de objetivos, sin alterar escala, NBT ni
  mecanica.
- El cliente entro en `Test` con Vulkan efectivo, REI y Jade. La granja de Slimes
  se comprobo en primer plano con suelo opaco; las granjas colocadas cargaron sin
  errores de modelos, texturas ni codigo de Trading Cells.

## Botin, escalas y recetas de granjas configurables del 2026-08-31

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `26/26` GameTests y produjeron un JAR identico de
  `2.244.615` bytes con SHA-256
  `5F6ADB727F25B8D538A080382F62F038B546CAAF62B0E9CD86EE29F630B17D2F`.
- `check` valido 384 JSON, 63 PNG, 13 payloads, 104 claves NBT, 91 recetas,
  arquitectura, dominio, contenido del JAR, portabilidad y neutralidad
  OpenGL/Vulkan.
- El GameTest nuevo muestrea hasta 1.000 ciclos reales del Piglin y 500 del
  Piglin Bruto: exige armas desgastadas, armadura de oro desgastada y hacha
  desgastada. El contrato de menu incluye ballesta, lanza, espada y las cuatro
  piezas de armadura, y excluye el disco imposible del Ghast.
- El validador de recursos fija las nuevas bases de Artropodos, Piglins y
  Shulkers y las matrices exactas de las quince recetas de generadores
  modificadas.
- El cliente de desarrollo entro en `Test` con Vulkan efectivo, REI y Jade,
  cargo las 91 recetas y registro 18 paneles de granjas configurables sin
  errores de modelos, texturas ni codigo de Trading Cells. No se movio la
  camara durante esta pasada, por lo que la comparacion visual de escalas sigue
  en la matriz manual.

## Correccion del menu de granjas configurables del 2026-08-31

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `25/25` GameTests y produjeron un JAR identico de
  `2.243.176` bytes con SHA-256
  `B2D6CDA9AE223DFED9EFB7C25AC9088B2FBCD6233DBE0B889F6C7EB4EB474A17`.
- `check` valido 384 JSON, 63 PNG, 13 payloads, 104 claves NBT, 91 recetas,
  arquitectura, dominio, contenido del JAR, portabilidad y neutralidad
  OpenGL/Vulkan.
- El GameTest nuevo construye el menu para cada objetivo de las diez familias
  configurables, rechaza categorias ausentes y confirma que espada y ballesta
  del Piglin usan la ruta de botin dinamico.
- En el mundo local `Test`, con Vulkan, REI y Jade, se abrio la Granja de
  Piglins que habia provocado la excepcion. El menu permanecio estable, mostro
  ambos objetos de equipo y `run/vulkan/logs/latest.log` no registro la NPE.

## Correcciones visuales y de recetas del 2026-08-31

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `24/24` GameTests y produjeron un JAR identico de
  `2.245.908` bytes con SHA-256
  `DB989E8727493474F0E35EB3D8727C1AA5444C2942F6BDC5D466EBA8E96EA7A7`.
- `check` valido 384 JSON, 63 PNG, 13 payloads, 104 claves NBT, 91 recetas,
  arquitectura, dominio, contenido del JAR, portabilidad y neutralidad
  OpenGL/Vulkan.
- El GameTest nuevo comprobo que las infusiones de Ghast y Happy Ghast conservan
  el cubo vacio despues de consumir lava o agua.
- En el mundo local `Test`, con OpenGL, REI y Jade, se verifico que el boton lila
  queda sobre la salida, el recetario queda junto al Infusor sin solaparse y una
  receta fantasma de 550 XP muestra `XP almacenada/550 XP`.
- El cliente cargo los modelos y texturas modificados sin errores de recursos de
  Trading Cells. La comprobacion visual manual de cada granja nueva en todos sus
  contextos sigue formando parte de la matriz pendiente.

## Ampliacion de granjas e Infusor del 2026-08-30

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `23/23` GameTests y produjeron un JAR identico de
  `2.244.401` bytes con SHA-256
  `1DE98FF217F1C433A660CF40C6105D8C5C057609AC69EA5F73822B2D2780BC59`.
- `check` valido 384 JSON, 63 PNG, 13 payloads, 104 claves NBT, 91 recetas,
  arquitectura, dominio, contenido del JAR, portabilidad y neutralidad
  OpenGL/Vulkan.
- Los GameTests cubren las diez familias configurables nuevas, sus catalogos
  dinamicos, capacidades laterales, persistencia y ciclos; tambien cubren las
  recetas dispersas del Infusor, colocacion simple y maxima, ingredientes con
  componentes exactos y recetas fantasma sin consumo.
- El cliente de desarrollo alcanzo el menu principal con OpenGL, REI y Jade,
  cargando el libro de recetas y los registros cliente sin errores de Trading
  Cells. Esta comprobacion de arranque no sustituye la inspeccion visual manual.

La matriz manual permanece pendiente. En particular, aun deben probarse Vulkan,
el cliente sin REI/Jade, todas las escalas y resoluciones, ambos idiomas, los
menus abiertos y la compatibilidad con mods externos.

## Auditoria de portabilidad del 2026-08-29

- Dos ejecuciones consecutivas de `clean releaseCheck` terminaron correctamente.
- Las dos ejecutaron `19/19` GameTests y produjeron un JAR identico de
  `2.096.734` bytes con SHA-256
  `D31076A2EC458960A4743047894A9BFDCCFD6E08A3452B19E27A7821475512FF`.
- `check` valido 282 JSON, 61 PNG, 13 payloads, 94 claves NBT, 59 recetas,
  arquitectura, dominio, contenido del JAR y neutralidad OpenGL/Vulkan.
- `checkPortability` inspecciono 865 archivos sin rutas absolutas operativas,
  shells externos, scripts auxiliares por SO, colisiones de case ni bytecode
  Python versionado.
- El microbenchmark portable del insertador completo 250.000 casos equivalentes
  en su smoke test (`44,007 ms` frente a `26,269 ms`, mejora `40,31 %`).
- La CI conserva la pasada completa en Ubuntu y añade `clean check` en Windows y
  macOS. Esa matriz remota debe quedar verde en el commit definitivo.

La evidencia local no sustituye la matriz manual. El arbol contiene los cambios de
mantenimiento de esta auditoria; tras crear el commit definitivo debe ejecutarse
`recordReleaseEvidence` una ultima vez para asociar el hash a ese commit limpio.

## Referencia previa del 2026-08-25

## Referencias

- Git HEAD conservado: `9fddde4894ea3c4040ab3c2257a4d2ff5f3c10d1`.
- JAR previo a esta pasada: `1.813.556` bytes, SHA-256
  `8C9EE1F547BC9E2D4AD5EB772611C16F3B9A4702C926893449098F9146C50EFA`.
- JAR candidato: `1.834.860` bytes, SHA-256
  `8D0F5990604406147F97AF91D1308BFBA4D0F56FB85C2F55A3F1779285E6DBA3`.
- Dos ejecuciones consecutivas de `clean releaseCheck` produjeron exactamente el
  mismo tamano y hash.
- El inventario completo y no destructivo del arbol se genera en
  `build/reports/release/release-audit.json` mediante `recordReleaseEvidence`.

## Validacion automatica aprobada

- Arquitectura hexagonal, reglas de dominio y neutralidad OpenGL/Vulkan.
- Validacion de 255 JSON, 59 PNG, traducciones, recetas, modelos y texturas.
- Un unico artefacto publicable y ausencia de GameTests, herramientas o recursos
  obsoletos dentro del JAR.
- Contrato 1.0.0: 13 IDs de payload, 94 claves NBT, 59 recetas y versiones 1 del
  esquema y protocolo de catalogos de granjas.
- Huella canonica del catalogo de recetas:
  `55582E9F91F9E0B8BCF262212CBA8D05351612D2F5A138B079A6EB479353486A`.
- 17 GameTests: round-trip de Block Entities, persistencia portatil, XP liquido,
  Infusor manual, payloads invalidos, capacidad lateral, salidas parciales,
  granjas sin botin, Fortuna 0/3/7/255 y Toque de Seda II con restauracion de
  datos de Block Entity. Las nuevas pruebas cubren tambien contenido arqueologico
  sin progreso de cepillado, porciones de tarta, rearme/suspension/redstone del
  generador de desafio y reutilizacion de arcas sin UUID permanentes.
- Servidor dedicado cargado con NeoForge `26.2.0.57` hasta `Done (0.270s)`.
- Smoke test RCON de 1.024 maquinas inactivas completado y cerrado correctamente.

## Rendimiento revalidado

- Insertador de salidas: mediana `38,247 ms` frente a `22,623 ms`, mejora
  `40,85 %`, con 3 ejecuciones de 250.000 casos equivalentes.
- Tooltips de niveles altos: mediana `32,939 ms` frente a `11,296 ms`, mejora
  `65,71 %`, con 5 ejecuciones de 2.000.000 casos equivalentes.
- Cache candidata del Autotrader: `7,458 ms` frente a `7,519 ms`, regresion
  `0,82 %`; permanece retirada.
- El smoke test de servidor produjo `1,691120` MSPT medio y `1,914873` p95. Una
  sola ejecucion confirma funcionamiento, no una mejora comparable.

## Cambios deliberadamente aplazados

No se dividieron `FarmerCropStackAdapter`, Trader, Autotrader ni las categorias
REI. El plan exige primero equivalencia manual, multijugador y plantillas de
rendimiento comparables; hacer esos refactors ahora aumentaria el riesgo sin una
medicion que los justifique.

Tampoco se conservaron nuevas optimizaciones de produccion. Las dos rutas ya
aceptadas se revalidaron y la unica hipotesis repetida del Autotrader no alcanzo
su umbral.

## Matriz manual pendiente

- OpenGL y Vulkan, con y sin REI, confirmando el backend efectivo.
- Espanol e ingles, escalas GUI 2-4 y resoluciones `1424x855`/`1920x1080`.
- Dos clientes, 2.304 intercambios, descuentos, restock, reinicio y XP.
- More Villagers y mods externos de cultivos, tiers, criaturas, cabezas, botin y
  tuberias.
- Copia de un mundo existente con comparacion NBT antes/despues.
- Instalacion del JAR en una instancia limpia y revision visual completa.

La candidata automatica esta aprobada. La publicacion 1.0.0 no se considera
certificada hasta completar y adjuntar la matriz manual de
`docs/RELEASE_CHECKLIST.md`.
